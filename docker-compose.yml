services:
  demo:
    build: .
    working_dir: /app
    command: ["python", "-m", "interopera.cli", "demo"]
    environment:
      INTEROPERA_NARRATIVE_PROVIDER: none
    volumes:
      - ./output:/app/output
